defaults:
- field_index_map_override: bc_only_override
well_base_path: dataset
wandb_data_name: ns_exponax
module_parameters:
  _target_: walrus.data.MixedWellDataModule
  batch_size: 100
  n_steps_input: 1
  n_steps_output: 1
  min_dt_stride: 1
  max_dt_stride: 1
  max_samples: 20000
  well_dataset_info:
    ns_exponax:
      include_filters: []
      exclude_filters: []
      path: dataset