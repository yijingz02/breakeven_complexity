defaults:
- field_index_map_override: bc_only_override
well_base_path: /data/well_format/
wandb_data_name: breakflow
module_parameters:
  _target_: walrus.data.MixedWellDataModule
  batch_size: 8
  inference_batch_size: 8
  single_frame_initialization: false
  n_steps_input: 10
  n_steps_output: 1
  min_dt_stride: 1
  max_dt_stride: 1
  max_samples: 20000
  well_dataset_info:
    breakflow:
      include_filters: []
      exclude_filters: []
      path: /data/well_format/breakflow
